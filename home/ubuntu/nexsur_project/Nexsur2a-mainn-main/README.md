# Nexora - منصة التواصل الاجتماعي المتقدمة

Nexora هي منصة تواصل اجتماعي حديثة وشاملة توفر مجموعة واسعة من الميزات المتقدمة للمستخدمين والمنشئين.

## 🚀 الميزات الأساسية

### 1. ملفات المستخدمين (User Profiles)
- ملفات شخصية قابلة للتخصيص مع صور الملف الشخصي والبنر
- مستويات الخصوصية (عام، أصدقاء فقط، خاص)
- عرض الإنجازات والشارات والحالة المُتحقق منها
- وضع المنشئ مع لوحة تحليلات وتحقق من الدخل

### 2. المنشورات المتقدمة (Advanced Posts)
- أنواع متعددة: نص، صور، فيديو، صوت، استطلاعات، قصص، ريلز
- جدولة المنشورات والمسودات
- وسم الموقع والهاشتاجات والإشارات
- خيارات التحقق من الدخل للمنشئين
- تسميات توضيحية بمساعدة الذكاء الاصطناعي
- منشورات تعاونية لعدة منشئين

### 3. الخلاصة الذكية (Smart Newsfeed)
- خلاصة مخصصة بناءً على الخوارزمية
- تسليط الضوء على المحتوى الرائج
- تصفية حسب المواضيع والمنشئين والاهتمامات
- توصيات الذكاء الاصطناعي
- أوضاع مختلفة (حسب الوقت مقابل الاهتمام)

### 4. التفاعلات والتعليقات (Reactions & Comments)
- تفاعلات متحركة وإيموجيات
- تعليقات متداخلة وردود مترابطة
- إشارات ووسوم وربط الهاشتاجات
- ترجمة تلقائية وفلتر السمية بالذكاء الاصطناعي
- ستة أنواع من التفاعلات (إعجاب، حب، ضحك، مندهش، حزين، غاضب)

### 5. الإشعارات (Notifications)
- إشعارات فورية وفي التطبيق والبريد الإلكتروني
- تخصيص التكرار والنوع
- تذكيرات الأحداث وتنبيهات نشاط الأصدقاء
- تنبيهات المحتوى

### 6. الإشراف على المحتوى (Content Moderation)
- كشف تلقائي بقوة الذكاء الاصطناعي
- نظام الإبلاغ من المستخدمين
- سير العمل الموافقة من المسؤول
- كشف البريد العشوائي والإساءة

### 7. البحث والاكتشاف (Search & Discovery)
- البحث عن الأشخاص والمنشورات والمجموعات والهاشتاجات
- الإكمال التلقائي والاقتراحات
- خيارات التصفية الذكية والبحث المتقدم

### 8. أدوات المنشئين (Creator Tools)
- لوحة تحليلات الوصول والمشاركة
- خيارات التحقق من الدخل والاشتراكات
- رؤى ديموغرافية الجمهور
- أدوات التعاون والنشر المشترك

## 📋 متطلبات النظام

- Node.js 18+
- npm أو yarn
- متصفح حديث (Chrome, Firefox, Safari, Edge)

## 🛠️ التثبيت والإعداد

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd nexora_platform
```

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. إعداد متغيرات البيئة
```bash
cp .env.example .env
```

### 4. تشغيل السيرفر
```bash
npm start
```

السيرفر سيعمل على `http://localhost:3000`

## 📁 هيكل المشروع

```
nexora_platform/
├── api/                    # API endpoints
│   ├── auth.js            # المصادقة والتسجيل
│   ├── users.js           # إدارة المستخدمين
│   ├── posts.js           # إدارة المنشورات
│   ├── comments.js        # إدارة التعليقات
│   ├── notifications.js   # إدارة الإشعارات
│   ├── search.js          # البحث والاكتشاف
│   └── creator.js         # أدوات المنشئين
├── database/              # قاعدة البيانات
│   ├── db.js             # تهيئة قاعدة البيانات
│   └── schema.js         # مخطط قاعدة البيانات
├── public/                # الملفات الثابتة
│   ├── index.html        # الصفحة الرئيسية
│   ├── styles.css        # أنماط CSS
│   └── script.js         # سكريبت JavaScript
├── server.js             # ملف السيرفر الرئيسي
├── package.json          # المكتبات والمعلومات
└── README.md            # هذا الملف
```

## 🔌 API Endpoints

### المصادقة
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/verify` - التحقق من التوكن

### المستخدمون
- `GET /api/users/:userId` - الحصول على ملف المستخدم
- `PUT /api/users/:userId` - تحديث الملف الشخصي
- `POST /api/users/:userId/follow` - متابعة مستخدم
- `POST /api/users/:userId/unfollow` - إلغاء متابعة
- `GET /api/users/:userId/followers` - قائمة المتابعين
- `GET /api/users/:userId/following` - قائمة المتابَعين

### المنشورات
- `POST /api/posts` - إنشاء منشور
- `GET /api/posts` - الحصول على الخلاصة
- `GET /api/posts/:postId` - الحصول على منشور واحد
- `PUT /api/posts/:postId` - تحديث منشور
- `DELETE /api/posts/:postId` - حذف منشور
- `POST /api/posts/:postId/react` - إضافة تفاعل
- `GET /api/posts/user/:userId` - منشورات المستخدم

### التعليقات
- `POST /api/comments` - إضافة تعليق
- `GET /api/comments/post/:postId` - تعليقات المنشور
- `PUT /api/comments/:commentId` - تحديث تعليق
- `DELETE /api/comments/:commentId` - حذف تعليق
- `POST /api/comments/:commentId/react` - تفاعل على تعليق

### الإشعارات
- `GET /api/notifications` - الحصول على الإشعارات
- `PUT /api/notifications/:notificationId/read` - وضع علامة كمقروء
- `PUT /api/notifications/read-all` - وضع علامة على الكل كمقروء
- `DELETE /api/notifications/:notificationId` - حذف إشعار
- `GET /api/notifications/preferences` - تفضيلات الإشعارات
- `PUT /api/notifications/preferences` - تحديث التفضيلات

### البحث
- `GET /api/search/users` - البحث عن المستخدمين
- `GET /api/search/posts` - البحث عن المنشورات
- `GET /api/search/hashtags` - البحث عن الهاشتاجات
- `GET /api/search/trending` - المواضيع الرائجة
- `GET /api/search` - بحث متقدم
- `GET /api/search/autocomplete` - اقتراحات تلقائية

### أدوات المنشئين
- `GET /api/creator/analytics` - تحليلات المنشئ
- `GET /api/creator/post/:postId/analytics` - تحليلات المنشور
- `GET /api/creator/monetization` - معلومات التحقق من الدخل
- `POST /api/creator/monetization/enable` - تفعيل التحقق
- `GET /api/creator/top-posts` - أفضل المنشورات
- `GET /api/creator/audience-stats` - إحصائيات الجمهور

## 🎨 تخصيص الواجهة

### الألوان
تم تصميم Nexora بلوحة ألوان حديثة:
- **الأزرق الكهربائي**: `#2A7CFF` (اللون الأساسي)
- **الأرجواني النيون**: `#A259FF` (اللون الثانوي)
- **الأسود الليلي**: `#0B0D12` (خلفية)
- **الأكوا السيبر**: `#36F1CD` (لون التركيز)

### اللغة
يدعم المشروع اللغة العربية بشكل كامل مع دعم RTL.

## 🔐 الأمان

- استخدام JWT للمصادقة
- تشفير كلمات المرور باستخدام bcryptjs
- CORS مُفعّل للطلبات الآمنة
- التحقق من الصلاحيات على كل endpoint

## 📊 التطوير المستقبلي

- [ ] دعم البث المباشر (Live Streaming)
- [ ] معالجة الفيديو المتقدمة
- [ ] نظام الرسائل المباشرة
- [ ] المجموعات والمجتمعات
- [ ] دعم الدفع والاشتراكات
- [ ] تطبيق الهاتف المحمول (iOS/Android)
- [ ] نظام التوصيات الذكي
- [ ] دعم WebSocket للتحديثات الفورية

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء فرع للميزة الجديدة
3. Commit التغييرات
4. Push إلى الفرع
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License.

## 📧 التواصل

للأسئلة والاقتراحات، يرجى التواصل عبر:
- البريد الإلكتروني: support@nexora.com
- الموقع الرسمي: https://nexora.com

---

**تم تطوير Nexora بـ ❤️ من قبل فريق Manus**
