# دليل المصادقة والتسجيل - Nexora Platform

## مقدمة

هذا الدليل يشرح كيفية عمل نظام المصادقة (تسجيل الدخول والتسجيل) في منصة Nexora، والتعديلات التي تم إجراؤها لإصلاح المشاكل.

---

## التعديلات التي تم إجراؤها

### 1. تحديث ملف `.env`

تم إضافة متغير `JWT_SECRET` الذي يستخدم لتوقيع وتحقق من رموز المصادقة (Tokens):

```env
MONGO_URI="mongodb+srv://aavbnfmhggirb_db_user:Aa14781478mn@nexsur2a.hcaimbe.mongodb.net/?retryWrites=true&w=majority&appName=Nexsur2a"
PORT=3000
JWT_SECRET="nexora-secret-key-2024-secure-authentication-token"
```

**الأهمية:** بدون `JWT_SECRET`، قد يحدث خطأ عند محاولة توليد رموز المصادقة.

### 2. تحويل واجهة تسجيل الدخول إلى الوضع الفاتح (Light Mode)

تم تحديث ملف `public/auth.html` لتغيير الألوان من الوضع الداكن إلى الوضع الفاتح:

*   **الخلفية:** من `#0B0D12` (أسود) إلى `#F5F5F5` (رمادي فاتح).
*   **حقول الإدخال:** من شفافة مع نصوص بيضاء إلى خلفية بيضاء مع نصوص سوداء.
*   **الحدود والظلال:** تم تقليل الشفافية والظلال لتناسب الوضع الفاتح.

---

## كيفية اختبار المصادقة

### المتطلبات

1.  **Node.js مثبت** على جهازك.
2.  **قاعدة البيانات MongoDB** متصلة (يتم الاتصال عبر `MONGO_URI` في ملف `.env`).
3.  **الخادم يعمل** على المنفذ 3000.

### خطوات الاختبار

#### الخطوة 1: التأكد من تشغيل الخادم

تأكد من أن الخادم يعمل بشكل صحيح. يجب أن تظهر رسالة مثل:

```
🚀 Nexora Server running on http://localhost:3000
📱 API available at http://localhost:3000/api
MongoDB Connected Successfully!
```

#### الخطوة 2: فتح صفحة تسجيل الدخول

افتح المتصفح وانتقل إلى:

```
http://localhost:3000
```

يجب أن تظهر واجهة تسجيل الدخول بالوضع الفاتح (Light Mode).

#### الخطوة 3: إنشاء حساب جديد

1.  انقر على الرابط **"إنشاء حساب جديد"** في الأسفل.
2.  أدخل البيانات التالية:
    *   **الاسم الكامل:** أحمد محمد
    *   **اسم المستخدم:** ahmed_2024
    *   **البريد الإلكتروني:** ahmed@example.com
    *   **كلمة المرور:** Password123!

3.  انقر على زر **"إنشاء الحساب"**.

**النتيجة المتوقعة:**
*   رسالة نجاح: "تم إنشاء الحساب بنجاح! يرجى تسجيل الدخول"
*   سيتم التبديل تلقائياً إلى نموذج تسجيل الدخول.

#### الخطوة 4: تسجيل الدخول

1.  أدخل البيانات التالية:
    *   **البريد الإلكتروني:** ahmed@example.com
    *   **كلمة المرور:** Password123!

2.  انقر على زر **"تسجيل الدخول"**.

**النتيجة المتوقعة:**
*   سيتم توجيهك إلى صفحة الخلاصة الرئيسية (`/home.html`).
*   سيتم حفظ التوكن (Token) في `localStorage` تحت اسم `token`.

---

## معالجة الأخطاء الشائعة

### 1. خطأ: "حدث خطأ في الاتصال بالخادم"

**السبب:** الخادم غير مشغل أو لا يمكن الوصول إليه.

**الحل:**
*   تأكد من تشغيل الخادم بأمر `npm start` أو `node server.js`.
*   تأكد من أن المنفذ 3000 متاح (لم يتم استخدامه من قبل تطبيق آخر).

### 2. خطأ: "Email or username already registered"

**السبب:** البريد الإلكتروني أو اسم المستخدم مسجل بالفعل.

**الحل:**
*   استخدم بريد إلكتروني أو اسم مستخدم مختلف.
*   أو احذف المستخدم من قاعدة البيانات (إذا كنت تريد إعادة الاختبار).

### 3. خطأ: "Invalid email or password"

**السبب:** البريد الإلكتروني أو كلمة المرور غير صحيحة.

**الحل:**
*   تأكد من إدخال البريد الإلكتروني وكلمة المرور بشكل صحيح.
*   تأكد من أن الحساب موجود (قم بإنشاء حساب جديد إذا لم يكن موجوداً).

### 4. خطأ: "MONGO_URI is not defined"

**السبب:** متغير `MONGO_URI` غير معرف في ملف `.env`.

**الحل:**
*   تأكد من أن ملف `.env` يحتوي على `MONGO_URI`.
*   تأكد من صحة رابط الاتصال بـ MongoDB.

---

## هيكل الملفات المتعلقة بالمصادقة

```
Nexsur2a-mainn-main/
├── api/
│   └── auth.js                 # مسارات API للمصادقة (login, register, verify)
├── database/
│   ├── db.js                   # اتصال MongoDB
│   └── models.js               # نموذج المستخدم (User Schema)
├── public/
│   ├── auth.html               # واجهة تسجيل الدخول والتسجيل (Light Mode)
│   ├── script.js               # كود JavaScript للتفاعل مع الواجهة
│   ├── home.html               # الصفحة الرئيسية بعد تسجيل الدخول
│   └── styles.css              # الأنماط العامة
├── .env                        # متغيرات البيئة (MONGO_URI, PORT, JWT_SECRET)
├── server.js                   # نقطة الدخول الرئيسية للخادم
└── package.json                # التبعيات والمكتبات المستخدمة
```

---

## كود المصادقة (ملخص)

### تسجيل مستخدم جديد (Register)

**المسار:** `POST /api/auth/register`

**البيانات المطلوبة:**
```json
{
  "username": "ahmed_2024",
  "email": "ahmed@example.com",
  "password": "Password123!",
  "displayName": "أحمد محمد"
}
```

**الاستجابة عند النجاح:**
```json
{
  "message": "User registered successfully",
  "userId": "user-id-here"
}
```

### تسجيل الدخول (Login)

**المسار:** `POST /api/auth/login`

**البيانات المطلوبة:**
```json
{
  "email": "ahmed@example.com",
  "password": "Password123!"
}
```

**الاستجابة عند النجاح:**
```json
{
  "message": "Login successful",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "user-id-here",
    "username": "ahmed_2024",
    "email": "ahmed@example.com",
    "displayName": "أحمد محمد",
    "avatarUrl": ""
  }
}
```

---

## الملاحظات المهمة

1.  **التوكن (Token):** يتم حفظ التوكن في `localStorage` تحت اسم `token` (لاحظ أن الكود الحالي يحفظه تحت اسم `authToken`، قد تحتاج إلى مراجعة هذا).

2.  **مدة صلاحية التوكن:** التوكن يصلح لمدة 7 أيام (`expiresIn: '7d'`).

3. **الأمان:** كلمات المرور يتم تشفيرها باستخدام `bcryptjs` قبل حفظها في قاعدة البيانات.

4. **CORS:** الخادم يدعم CORS، مما يسمح بطلبات من نطاقات مختلفة.

---

## الخطوات التالية

إذا كنت تواجه مشاكل أخرى أو تريد إضافة ميزات جديدة (مثل تسجيل الدخول عبر Google)، يرجى إبلاغي.
